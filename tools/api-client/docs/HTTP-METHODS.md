# HTTP methods

GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS are supported. Bodies are
suppressed for GET/HEAD. Method colors in the sidebar follow a consistent
palette defined by theme tokens (`--m-get`, `--m-post`, …).
