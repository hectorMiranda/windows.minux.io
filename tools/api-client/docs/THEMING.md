# Theming

Themes are pure CSS that set design-token variables on `[data-theme="…"]`.
Ship with dark, light, midnight, nord, solarized, dracula, github and gruvbox.
Add one by copying `css/themes/dark.css`, renaming the selector, and listing it
in `THEMES` in `js/app.js`.
