# Collections

Group related requests into a collection; nest with folders. Collections are
saved to `localStorage` and can be exported to a single JSON file for review or
version control. Import from Postman v2.1, OpenAPI 3, or a raw cURL command.
