# Code generation

Open any request and run **Generate code…** (Ctrl+K → "Generate"). The
request is first resolved (variables interpolated, auth applied) then handed to
a per-language generator. Supported targets include cURL, HTTPie, wget, fetch,
Axios, XHR, Node, Python, Go, Ruby, PHP, C#, Java, PowerShell and Rust.
