# Roadmap

- WebSocket & SSE inspectors
- GraphQL query builder with schema introspection
- gRPC reflection client
- Request chaining UI (use a response field as the next request's input)
- Mock server from a collection
- Diff two responses side by side
